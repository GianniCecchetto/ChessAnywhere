name: CI - Tests python

on:
  push:
    paths:
      - "python-app/**"
  pull_request:
    branches:
      - main     # run on PRs to main

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          pip install -r python-app/requirements.txt
          pip install pytest

      - name: Run tests
        run: PYTHONPATH=python-app pytest python-app/test
